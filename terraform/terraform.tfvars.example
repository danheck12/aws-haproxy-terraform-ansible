aws_region    = "us-east-2"
name          = "haproxy-lab"
instance_type = "t3.micro"

# Must already exist in EC2 -> Key Pairs
ssh_key_name  = "YOUR_KEYPAIR_NAME"

# Your public IP /32 for SSH, e.g. 203.0.113.10/32
admin_cidr    = "YOUR_PUBLIC_IP/32"
